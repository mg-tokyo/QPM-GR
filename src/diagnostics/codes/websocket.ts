import type { ErrorCodeDefinition } from '../types';

// Keep in sync with CURRENT_VERSION in ../codes.ts (local copy avoids a
// circular import).
const V = '3.2.29';

export const WEBSOCKET_CODES: readonly ErrorCodeDefinition[] = [
  {
    code: 'QPM-WS-001',
    subsystem: 'websocket',
    category: 'core',
    severity: 'warn',
    title: 'No active connection',
    description: 'sendRoomAction() called before a room connection was established.',
    userAction: 'Reconnect to the game (refresh the tab if it persists).',
    devNotes: 'src/websocket/api.ts — guards against missing MagicCircle_RoomConnection.',
    sinceVersion: V,
    // §9 — fires only during user-driven sends; user can act (reconnect/refresh); not transient
    // (each fire is a real failed action). Default warn throttle (30s) suppresses send-spam.
    notifyUser: true,
  },
  {
    code: 'QPM-WS-002',
    subsystem: 'websocket',
    category: 'core',
    severity: 'info',
    title: 'Throttled',
    description: 'sendRoomAction() was throttled by the per-key rate limit.',
    devNotes: 'src/websocket/api.ts — throttle bucket per (type, key).',
    sinceVersion: V,
  },
  {
    code: 'QPM-WS-003',
    subsystem: 'websocket',
    category: 'core',
    severity: 'warn',
    title: 'WebSocket send failed',
    description: 'sendRoomAction() returned ok:false from the underlying connection.',
    devNotes: 'src/websocket/api.ts — non-ok return path.',
    sinceVersion: V,
  },
  {
    code: 'QPM-WS-004',
    subsystem: 'websocket',
    category: 'core',
    severity: 'warn',
    title: 'Invalid payload',
    description: 'sendRoomAction() rejected a payload that failed validation.',
    devNotes: 'src/websocket/api.ts — validatePayload(); likely a caller bug.',
    sinceVersion: V,
  },
  {
    code: 'QPM-WS-005',
    subsystem: 'websocket',
    category: 'core',
    severity: 'info',
    title: 'Locker blocked send',
    description: 'A registered preflight (locker guard) blocked the send.',
    devNotes: 'src/websocket/api.ts — registerSendPreflight() returned ok:false.',
    sinceVersion: V,
  },
  {
    code: 'QPM-WS-006',
    subsystem: 'websocket',
    category: 'core',
    severity: 'warn',
    title: 'QuinoaCommand rejected',
    description: 'The server returned ok:false for a QPM-originated QuinoaCommand envelope.',
    devNotes: 'src/websocket/commandSequencer.ts — context.code is the server code (invalid_sequence retries once).',
    sinceVersion: '3.3.31',
  },
  {
    code: 'QPM-WS-007',
    subsystem: 'websocket',
    category: 'core',
    severity: 'warn',
    title: 'QuinoaCommand result timeout',
    description: 'No QuinoaCommandResult arrived within 5 s — outcome unknown (stale/duplicate sequences are dropped silently).',
    devNotes: 'src/websocket/commandSequencer.ts — usually a sequence desync; check getCommandSequencerStats().',
    sinceVersion: '3.3.31',
  },
  {
    code: 'QPM-WS-008',
    subsystem: 'websocket',
    category: 'core',
    severity: 'warn',
    title: 'Command sequencer attach failed',
    description: 'Could not wrap the room connection send functions; QPM falls back to legacy flat sends.',
    devNotes: 'src/websocket/commandSequencer.ts ensureAttached().',
    sinceVersion: '3.3.31',
  },
  {
    code: 'QPM-WS-009',
    subsystem: 'websocket',
    category: 'core',
    severity: 'warn',
    title: 'Transport allowlist out of date',
    description: 'The game (or a not_ackable verdict) showed a type uses a different transport than LEGACY_ROOM_ACTION_TYPES predicts. QPM mirrors the observed transport automatically.',
    devNotes: 'src/websocket/transport.ts — update LEGACY_ROOM_ACTION_TYPES in envelope.ts; run __QPM_INTERNAL__.commandSequencer.transportAudit().',
    sinceVersion: '3.3.31',
  },
  {
    code: 'QPM-WS-010',
    subsystem: 'websocket',
    category: 'core',
    severity: 'warn',
    title: 'Send budget exhausted',
    description: 'A QPM action was refused locally (reason: throttled) because QPM\'s share of the socket-wide command budget (server: ~300 per ~10 s) was used up. A feature is sending in a tight loop.',
    devNotes: 'src/websocket/transport.ts takeSendToken() — 30 burst, 10/s sustained. Check __QPM_INTERNAL__.commandSequencer.sendBudget().',
    sinceVersion: '3.3.31',
  },
  {
    code: 'QPM-WS-011',
    subsystem: 'websocket',
    category: 'core',
    severity: 'warn',
    title: 'Envelope skipped pre-session',
    description: 'A QuinoaCommand envelope reached sendMessage before Welcome; the sequencer skipped rewrite (fires once per session) so a burned commandSequence would not desync subsequent envelopes.',
    devNotes: 'src/websocket/commandSequencer.ts — check stats().skippedPreSession. A non-zero count means QPM built an envelope before isCommandSessionReady turned true.',
    sinceVersion: V,
  },
  {
    code: 'QPM-WS-012',
    subsystem: 'websocket',
    category: 'core',
    severity: 'warn',
    title: 'QuinoaCommand dropped (stale)',
    description: 'A room frame advanced past this envelope\'s commandSequence without a matching QuinoaCommandResult. The server dropped the send silently (rule: stale/duplicate numbers → no result). Definite rejection; the caller may choose to resend.',
    devNotes: 'src/websocket/commandSequencer.ts — grace: qpm.ws.sequencer.staleGraceMs (default 750 ms). Toggle: qpm.ws.sequencer.staleDetect.enabled. Check stats().dropped and stats().idleResyncs (a drop triggers CS-1 heal when nothing else is outstanding).',
    sinceVersion: V,
  },
];
