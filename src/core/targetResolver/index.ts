export { resolveTarget, explainTarget, listTargetRecipes, untrackTarget } from './resolver';
export { buildResolverProxy } from './resolverProxy';
export type {
  ResolveTargetEvidence,
  ResolveTargetParams,
  ResolveTargetResult,
  TargetRecipe,
  TargetRecipeSignal,
  TargetRecipeSignalType,
} from './types';
